# Disable the Spring banner to keep STDOUT clean for the CLI
spring.main.banner-mode=off

# Configure the custom prompt as requested in the assignment
spring.shell.prompt.provider.enabled=true
shell.prompt=#